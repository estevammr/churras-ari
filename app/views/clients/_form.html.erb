<%= form_for @client, :html => { :class => "form-horizontal client" } do |f| %>

    <% if @client.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@client.errors.count, "error") %> prohibited this client from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @client.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :nome_do_cliente, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:name]) %>
  </div>
  <div class="control-group">
    <%= f.label :nascimento, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :birthday, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:birthday]) %>
  </div>
  <div class="control-group">
    <%= f.label :sexo, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :gender, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:gender]) %>
  </div>
  <div class="control-group">
    <%= f.label :endereço, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :place, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:place]) %>
  </div>
  <div class="control-group">
    <%= f.label :telefone, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :phone, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:phone]) %>
  </div>
  <div class="control-group">
    <%= f.label :email, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :email, :class => 'form-control' %>
    </div>
    <%= error_span(@client[:email]) %>
  </div>

  <br/>
  <%= f.submit current_page?(new_client_path) ? 'Criar' : 'Editar', :class => 'btn btn-primary' %>
  <%= link_to t('.cancelar', :default => t("helpers.links.cancelar")),
            clients_path, :class => 'btn btn-default' %>

<% end %>
