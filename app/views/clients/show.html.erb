<%- model_class = Client -%>
<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Detalhes do Cliente</a>
    </div>
  </div><!-- /.container-fluid -->
</nav>

<dl class="dl-horizontal">
  <dt><strong><%= model_class.human_attribute_name(:nome_do_cliente) %>:</strong></dt>
  <dd><%= @client.name %></dd>
  <dt><strong><%= model_class.human_attribute_name(:data_de_nascimento) %>:</strong></dt>
  <dd><%= @client.birthday %></dd>
  <dt><strong><%= model_class.human_attribute_name(:sexo) %>:</strong></dt>
  <dd><%= @client.gender %></dd>
  <dt><strong><%= model_class.human_attribute_name(:endereço) %>:</strong></dt>
  <dd><%= @client.place %></dd>
  <dt><strong><%= model_class.human_attribute_name(:telefone) %>:</strong></dt>
  <dd><%= @client.phone %></dd>
  <dt><strong><%= model_class.human_attribute_name(:email) %>:</strong></dt>
  <dd><%= @client.email %></dd>
</dl>

<%= link_to t('.voltar', :default => t("helpers.links.voltar")),
              clients_path, :class => 'btn btn-default'  %>
<%= link_to t('.editar', :default => t("helpers.links.editar")),
              edit_client_path(@client), :class => 'btn btn-default' %>
<%= link_to t('.deletar', :default => t("helpers.links.deletar")),
              client_path(@client),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
